import { PublicTraderRecord } from "@/services/trader.service";

/**
 * Public trader model types.
 * All trader data is fetched dynamically from the TradeSlot backend API via traderService.
 */
export type MockTrader = PublicTraderRecord;
