// Left intentionally minimal for the MVP: the flat fee is a constant, not a
// persisted/configurable rule, so there's nothing to query yet. This file
// exists so a future pricing-rules table has a home without restructuring
// the module.
export const billingRepository = {};